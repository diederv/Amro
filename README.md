# AMRO

Android take-home assessment — trending movies via TMDB.

See [`AMRO_Project_Brief.md`](AMRO_Project_Brief.md) for the full plan.
A proper README lands in Phase 7.

## Quick start (dev)

1. `cp local.properties.example local.properties` and fill in `sdk.dir` + `TMDB_API_KEY`.
2. `./gradlew :app:assembleDebug`
